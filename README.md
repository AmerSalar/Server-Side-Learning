# I'm learning Node :)

This is completely for my journey in learning Node.js and Express.js, so the content is pretty much the basics

Day 1: Made repo and connected my IDE to my Github, covered basics of node.js, i found out that we don't have window and DOM here, but we have global variables. Modules: here in Node our files are modules by default.

Day 2: More on module & require, i learned that require can return an object of functions or we can use it as void (simply not asigning the reference of the function to anything), when we require the module that has the function called inside it. Node has some built in modules like (OS, PATH, FS, HTTP....etc)

Day 3: Exploring the OS, and PATH modules
